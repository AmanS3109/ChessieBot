"""rag package: small marker file so the directory is importable as a package.

This file is intentionally minimal. If you want package-level initialization,
add it here (for example, package metadata or convenient helpers).
"""

__all__ = ["data_loader", "generator", "retriever", "utils"]
